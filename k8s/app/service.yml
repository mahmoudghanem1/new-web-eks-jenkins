apiVersion: v1
kind: Service
metadata:
  name: new-web-service
  labels:
    app: new-web
spec:
  type: NodePort
  selector:
    app: new-web
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30007